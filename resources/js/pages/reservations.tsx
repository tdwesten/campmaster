import Layout from '@/layouts/app-layout'
import { Head } from '@inertiajs/react'

export default function reservations({}) {
    return (
        <Layout>
            <Head title="Reservations"/>

            <div className={`bg-primary`}>asdsds</div>
        </Layout>
    )
}
